# VLM Refinement Outputs

Esta pasta guarda os resultados da ronda 13.

Olhar primeiro para:

```text
CURRENT_RUN.txt
current/
current/RUN_README.md
current/review/
```

As runs novas ficam em:

```text
runs/<timestamp>_ronda_13_codex_<mode>/
```

Dentro de cada run, `review/` e a vista limpa. Os outros ficheiros sao os artefactos brutos usados pela pipeline.

